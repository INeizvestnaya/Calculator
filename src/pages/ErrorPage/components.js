import styled from 'styled-components';

const ErrorPageLabel = styled.div`
  padding-top: 5%;
  text-align: center;
  font-weight: bold;
  color: ${({ theme }) => theme.colors.font};
`;

export { ErrorPageLabel };
